{
  "devDependencies": {
    "tailwindcss": "^3.4.15"
  },
  "scripts": {
    "build": "mkdir -p dist && tailwindcss -i ./src/input.css -o ./dist/output.css --minify && cp ./src/index.html ./dist/"
  }
}
